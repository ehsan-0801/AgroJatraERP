# Architecture

Frontend: Feature-based React + Vite + TypeScript.

State: Zustand (UI), TanStack Query (server state).

Backend: Supabase Auth + PostgreSQL + Storage.

Security: Protected routes, RLS, validation, reusable services.
